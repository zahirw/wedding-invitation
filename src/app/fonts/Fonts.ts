//font
import localFont from "next/font/local";

const DearMother = localFont({ src: "DearMother.ttf" });
const LTRemark = localFont({ src: "LTRemark.otf" });
const Cinzel = localFont({ src: "Cinzel.ttf" });

export { DearMother, LTRemark, Cinzel };
